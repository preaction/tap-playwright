{
  "name": "@preactionme/tap-playwright",
  "version": "0.0.1",
  "description": "Test-Anything Protocol (TAP) output for Playwright Test",
  "main": "lib/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "playwright test -c t",
    "build": "tsc"
  },
  "keywords": [ "playwright", "tap" ],
  "author": "Doug Bell",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.18.1",
    "typescript": "^4.5.5"
  }
}
